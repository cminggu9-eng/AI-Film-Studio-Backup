# Director Native Type Positive Test Report V0.1

Result: **20 / 20 PASS**, provider calls: 0.

The suite confirms complete compiled-to-wire provenance, final 15-field schema identity, strict wire mechanics, native array and exact `ABSENT` acceptance, R26 fail-closed replay, all container field domains, unreachable legacy string path, schema-derived generic reminder, Repair09K state-object regression, Repair20 Scene Writer regression, mandatory gate results, and 7/7 canonical plus 6/6 lock integrity.

The resulting type manifest has `outer_field_count: 15`; `unresolved_decisions` is `ABSENT | native JSON array<JSON string>`, while `relevant_prior_state` and `current_state` are native JSON objects.

