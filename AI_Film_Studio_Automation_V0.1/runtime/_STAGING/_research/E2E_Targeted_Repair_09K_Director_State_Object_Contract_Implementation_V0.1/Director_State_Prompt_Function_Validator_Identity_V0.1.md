# Director State Prompt Function Validator Identity V0.1

Result: **PASS** (`DIR-OBJ-09`, `DIR-OBJ-NEG-14`, `DIR-OBJ-NEG-15`).

All three surfaces retain the same exact 15 outer fields, dynamic object field identities, exact-ABSENT field identities, canonical mode/outcome domains, compiled state schema hash, and source trace hash. Provider-neutral `const` and DeepSeek typed-enum are treated as representation-equivalent projections of the same compiled semantic contract.

The system prompt is derived from the final function parameters and includes the exact run-specific nested schemas. It instructs exact copying from `director_state_projection`, exact `ABSENT` for the three frozen fields, and explicitly forbids JSON encoding an object as a string. Outer `scene_packages`, `canon_assignment_locks`, and `prohibited_changes` remain excluded.
