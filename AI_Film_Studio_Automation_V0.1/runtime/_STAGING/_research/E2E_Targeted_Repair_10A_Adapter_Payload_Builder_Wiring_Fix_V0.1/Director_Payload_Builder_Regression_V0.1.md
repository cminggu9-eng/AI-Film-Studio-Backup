# Director Payload Builder Regression

Provider-free request capture remains strict: `submit_director_package`, full machine-field schema, canonical control constraints, and matching structured/provider schema hashes.
