# Showrunner Live Request Construction Regression

Provider-free live-path capture: Showrunner, non-structured branch explicitly `None`, policy budget and final payload `max_tokens=4500`, valid JSON-output request, network send not reached. Mocked response interpretation also passed without NameError.
