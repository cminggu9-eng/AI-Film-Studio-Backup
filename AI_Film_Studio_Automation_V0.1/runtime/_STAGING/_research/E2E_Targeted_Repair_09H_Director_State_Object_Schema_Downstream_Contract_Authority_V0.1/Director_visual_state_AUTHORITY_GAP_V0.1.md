# Director visual_state Authority Gap V0.1

## Lawful purpose

The field preserves source-attributed visual/spatial continuity necessary for Direction and downstream comparison. It cannot set Art Director production design Canon, Character & Acting method, or Showrunner story facts.

## Missing authority

Compiled fixtures provide dynamic state dimensions; the current fixture supplies `clothing_visual_state_code` values. The Scene Writer scene envelope has three visual keys. Probe04 and R13 use incompatible visual maps. No authorized contract says which of these, if any, is the exact Director `visual_state` schema.

## Decision required

Specify whether this is an exact compiled-state projection, a source reference, or an independent Director continuity view, then identify only the visual/spatial fields that existing authority requires.

