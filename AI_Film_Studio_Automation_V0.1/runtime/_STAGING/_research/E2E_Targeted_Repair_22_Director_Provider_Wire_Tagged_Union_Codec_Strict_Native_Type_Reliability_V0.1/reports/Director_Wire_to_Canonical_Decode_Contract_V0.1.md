# Wire-to-canonical decode contract

Validated `ABSENT` decodes to canonical `ABSENT`; validated `PRESENT` decodes to its native array, including `[]`. Decode failure taxonomy: `PROVIDER-WIRE CODEC CONFORMANCE FAILURE`.
