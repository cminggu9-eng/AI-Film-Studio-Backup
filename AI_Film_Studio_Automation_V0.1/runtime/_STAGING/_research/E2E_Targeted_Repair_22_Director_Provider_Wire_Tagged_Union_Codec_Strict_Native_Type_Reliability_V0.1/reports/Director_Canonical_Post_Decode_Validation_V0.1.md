# Canonical post-decode validation

Only decoded arguments reach `make_director_payload_validator`. It remains the authoritative exact 15-field validator. Failure taxonomy: `DIRECTOR CANONICAL STRUCTURED OUTPUT CONFORMANCE FAILURE`.
