# Director DeepSeek tagged-wire Probe22

Probe `DIRECTOR-TAGGED-WIRE-PROBE-22-F03` made exactly one DeepSeek call (HTTP 200, `tool_calls`, not truncated). The raw 15-field arguments contained `unresolved_decisions: {status: PRESENT, items: [...]}`. Provider-wire validation, deterministic decode, and canonical validation all passed. It then stopped at `HANDOFF CONTRACT FAILURE — Received locks changed in transit`: the standalone probe constructed its canonical validator with the fixture's one lock but supplied the frozen R26 six-lock set to the subsequent handoff check. The live runtime passes the same Scene Writer lock set to both operations; this was a Probe22 harness construction mismatch, not a provider-wire failure. No retry, fallback, or R03 restart occurred. Raw SHA-256: `d9f743c15c218624788639eaaa6d84c2c0dff239b24c2fbe9ac174649ab3a3d6`.
