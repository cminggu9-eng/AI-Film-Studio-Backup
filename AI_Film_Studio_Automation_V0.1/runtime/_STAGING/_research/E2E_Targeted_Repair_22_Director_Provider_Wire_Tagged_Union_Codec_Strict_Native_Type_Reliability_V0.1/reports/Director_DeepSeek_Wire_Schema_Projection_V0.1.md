# DeepSeek wire schema projection

The strict forced function retains `submit_director_package`, all 15 required outer fields, and native object state fields. Only the approved union becomes the fixed strict object; no canonical skill or state ledger changed.
