# Director Deterministic Display Projection

The eight fields render in canonical heading order as `heading + newline + exact submitted value`. Projection has no extraction, summary, semantic fill, merge, or inference.
