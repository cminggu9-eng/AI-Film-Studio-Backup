# Director Structured Payload to Envelope Contract

Validated model fields + deterministic runtime metadata + exact upstream locks/prohibitions form the downstream envelope. Integration does not derive semantics, rewrite state, or create metadata through model output.
