# Director Provider Compatibility Projection Contract

`DIRECTOR_DEEPSEEK_STRICT_COMPATIBILITY_PROJECTION_V0.1` preserves every required field, the eight machine fields, exact mode/state domains, ABSENT-only flags/handoffs, state dimensions, and carry-forward boundary. It projects representation only; it adds/removes no semantics.
