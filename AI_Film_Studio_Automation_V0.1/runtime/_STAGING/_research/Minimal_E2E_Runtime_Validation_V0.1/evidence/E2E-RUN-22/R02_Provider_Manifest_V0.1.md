# R02 Provider Manifest V0.1

Two calls completed before safe stop: Showrunner non-strict and Scene Writer strict. Both returned HTTP 200, persisted raw-first evidence, passed truncation checks, and used no retry/fallback.

Director and the remaining four downstream roles were NOT REACHED. Canonical record: `provider_manifest.json`.
