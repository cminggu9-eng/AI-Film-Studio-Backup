# R03 State-Field Projection Evidence

The strict Scene Writer payload uses F03-only field `camera_battery_state`, with the exact token domain `battery_installed` and `battery_removed_and_sealed`, and authorized transition `battery_removal_and_seal`.

Evidence: `artifacts/scene_writer_final_wire_payload.json`.
