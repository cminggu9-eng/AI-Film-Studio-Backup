# R03 Entity Identity Projection Evidence

The F03 compiled contract provides entity identity `C-09`; the Scene Writer provider schema constrains `state.entity_id` to exactly `C-09`.

Semantic Safeguard identity evaluation was not reached because strict Scene Writer schema validation failed first.
