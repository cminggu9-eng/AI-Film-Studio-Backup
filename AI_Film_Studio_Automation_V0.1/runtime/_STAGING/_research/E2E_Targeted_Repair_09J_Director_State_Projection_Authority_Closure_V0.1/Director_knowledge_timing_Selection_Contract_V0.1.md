# Director knowledge_timing Selection Contract V0.1

Only a closed machine-readable compiled knowledge constraint or validated knowledge-event record can be projected. Keys, requiredness, types, and tokens must be inherited without renaming.

Current fixture contracts carry knowledge-event data but do not close an event's internal schema for strict projection. Existing knowledge locks remain authoritative in their original lock/handoff channels. Consequently this Director field is exactly:

```json
{"type":"string","enum":["ABSENT"]}
```

Here `ABSENT` means no authorized projectable knowledge record for this field; it never means unknown knowledge.

Status: **AUTHORITY CLOSED**.

