# 定向返工 Round 1

日期：2026-08-22  
触发：首轮证据红队、内容边界审核与 TEST A–N 独立复跑均未完全通过。  
范围：只修复被定位的问题，不重蒸馏、不扩大能力、不修改 Vault。

## 修复项

1. TEST A：Core Clause 恢复为保留“将进一步”及三个具体对象；“有效”验收依据单列 Meaning Warning；补 Showrunner Handoff。
2. TEST B：Core Clause 与 Meaning Lock 保留“分析已构成依据”的既有时序，以及“将 / 重新 / 之间”的未来、迭代、关系范围含义；Rewrite 同步保留。
3. TEST F：Meaning Lock 与 Rewrite 保留“我们确认”、测试记录“均”已归档的全称状态、`具备复审条件` 和 `可以进入` 两个不同状态，并显式保持三项结果到复审结论的因果。
4. Nominalization provenance：YGZ-01/04 直接支持泛化动词 + 抽象名词；YGZ-05/PAPER 直接支持抽象名词展开；台账拆为 M2a/M2b。
5. Anti-Purism：补回 Charter 的 Frequency，并明确它不是数值硬门。
6. QA/Rewrite 契约：统一使用完整状态 `CONTEMPORARY USAGE UNKNOWN`。
7. Late Information Release：专业法律/定义例外明确标为 AI FILM STUDIO SYNTHESIS。
8. TEST I/N：补历史候选信号边界、Register 与置信度。

## 待复测

- TEST A、B、F Meaning 回归。
- Nominalization 归因和 ledger 一致性。
- Anti-Purism 判定链与 Modernity 输出契约。

在复测 PASS 前，主稿保持 `under_review / pending`。
