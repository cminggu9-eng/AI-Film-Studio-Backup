# Character & Acting Failure Classification Contract V0.1

Date: 2026-08-31

If the Character role payload is lawful but Integration has not assembled its reserved slot, or a raw validator incorrectly applies the final ten-field contract, classify:

`INTEGRATION MACHINE-CONTRACT / TRANSPORT-SLOT ALIGNMENT FAILURE`

Do not classify it `ROLE SEMANTIC FAILURE`.

Use Character semantic/token/authority classifications only for actual invalid outcomes, forbidden ownership takeover, absent Character-owned machine fields, or real canonical violations. Provider, HTTP, truncation, parse, raw-role, assembly, final-transport, and handoff failures remain separate stages.

E2E-RUN-16's original `EXECUTOR FAILURE` record remains immutable; the Repair13 replay is additive corrected attribution only.

