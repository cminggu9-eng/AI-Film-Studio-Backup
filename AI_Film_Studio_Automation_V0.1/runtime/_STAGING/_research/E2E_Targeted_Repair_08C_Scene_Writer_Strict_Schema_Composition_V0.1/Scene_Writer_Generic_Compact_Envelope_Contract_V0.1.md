# Generic Compact Envelope Contract

The envelope owns `format`, compact control/state/scenes shape, canonical control tokens, required fields, and `additionalProperties:false`; it contains no fixture semantic enum after projection preparation.
