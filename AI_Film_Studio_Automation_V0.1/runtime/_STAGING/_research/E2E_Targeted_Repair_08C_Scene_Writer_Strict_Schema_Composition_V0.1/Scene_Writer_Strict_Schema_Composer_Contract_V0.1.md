# Strict Schema Composer Contract

`compose(compiled)` is deterministic and fail-closed. It may constrain generic compact schema only with compiled fixture scene/entity/state/transition values; it invents nothing.
