# Schema Composition Provenance Audit

Generic compact envelope comes from the validated strict transport contract; composer removes its historical fixture enums then projects fixture scene IDs, entities, state values, and transitions from Repair 07 compiled contract. `format` remains transport-owned.
