# Scene Writer Live Compiled Schema Wiring Contract V0.1

Missing binding or invalid compiled schema fails closed. `submit_scene_writer_package` remains the sole forced tool; the compiled schema is the only parameters source.
