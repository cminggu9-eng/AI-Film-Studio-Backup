# Strict Transport Authorization Registry V0.1

The lifecycle registry is closed and exact:

| Role | Function |
| --- | --- |
| Scene Writer | `submit_scene_writer_package` |
| Director | `submit_director_package` |

No wildcard, prefix, substring, function-only, or role-only admission exists. Every other role/function pair is unauthorized.
