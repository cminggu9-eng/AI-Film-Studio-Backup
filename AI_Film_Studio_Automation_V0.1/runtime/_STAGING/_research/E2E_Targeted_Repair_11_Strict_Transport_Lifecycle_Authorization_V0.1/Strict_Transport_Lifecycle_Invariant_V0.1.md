# Strict Transport Lifecycle Invariant V0.1

Strict transport may only be used by an explicitly authorized role/function pair. For each strict invocation, lifecycle validation requires: exact registry membership, strict enabled, matching tool choice, adapter-scoped Provider mechanics, verified required tool call, parsed arguments, and schema validation.

An authorized pair with strict disabled, or an unauthorized pair with strict enabled, fails lifecycle consistency.
