# Continuity primary_state_or_outcome Alignment V0.1

`primary_state_or_outcome` is the sole primary Continuity machine-outcome source in the current integration contract. Validation order is transport → machine field → canonical token → State Evidence → explicit machine signals if a future authoritative field exists → authority boundary. No competing outcome field exists, so no winner/merge rule is needed.

