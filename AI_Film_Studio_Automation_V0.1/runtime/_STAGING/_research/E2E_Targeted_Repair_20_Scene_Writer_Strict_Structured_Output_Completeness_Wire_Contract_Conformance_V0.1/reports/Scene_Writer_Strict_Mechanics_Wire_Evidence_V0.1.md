# Scene Writer strict mechanics wire evidence

Before send, the harness builds the final DeepSeek payload through the approved adapter, then records:

1. exact function name;
2. strict true;
3. exact forced tool choice;
4. payload and parameter hashes;
5. five-way schema and required-set identity.

The R25 payload already satisfied all four transport mechanics. The successful fresh probe also satisfied them, proving the Repair20 change addresses provider instruction completeness rather than changing strict authorization or adapter ownership.
