# Scene Writer Compact Hydration State Bridge V0.1

Result: PASS.

Flow: compact tool arguments -> current-run state projection -> exact field-set validation -> entity/token/transition validation -> hydration into the exact projected state field -> final Scene Writer package.

The compact state uses neutral transport fields (`entity_id`, `custody`, `state_display`) plus the dynamically projected machine-state field. Hydration no longer writes a fixed clothing field and cannot infer ownership from token prefixes, prose, scene content, or keywords.

R21 replay hydrated `signboard_on`, `signboard_on`, `signboard_off` into `signboard_state` without token mutation.
