# Shared QA Validation Fixture Pack V0.1

所有文本均为本轮原创的 `VALIDATION / SYNTHETIC / NON-CANON` 夹具，不是 Project-001、Scene Writer、Canon 或真实私人对话。Frozen Core 将在用户 Human Acceptance 后以 manifest hash 冻结；Rolling 夹具只可在未来验证周期以新版本新增，不改写本版本记录。

Production Runtime runner 只读取每个 fixture 的 `## Text` 与 `## Runtime Context`；不得读取 staging 中的 Gold Criteria、Automated Evaluation 或 Human Review。

