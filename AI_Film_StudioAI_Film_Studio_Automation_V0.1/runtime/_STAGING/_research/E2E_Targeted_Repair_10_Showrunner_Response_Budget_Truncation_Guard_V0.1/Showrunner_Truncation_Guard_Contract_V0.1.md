# Showrunner Truncation Guard Contract

Raw response and usage persist before parsing. `finish_reason=length`, cap contact, or incomplete JSON blocks parsing with RESPONSE-BUDGET failure. No brace append, continuation, retry, fallback, or semantic reconstruction is permitted.
